const activeUsers: string[] = [];
activeUsers.push("wee");

const bools: Array<boolean> = [];

type Pointt = {
  x: number;
  y: number;
};

const coords: Pointt[] = [];
coords.push();

const board: string[][] = [["X"]];
